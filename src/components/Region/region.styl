.region {
	// padding: 15px;
	// margin-bottom: 8px;
	// background-color: lighten(#ff8100, 90%);

	padding: 20px 15px 20px 0px;
	border-bottom: 1px solid #dedede;

	&:last-child {
		border-bottom: none;
	}
}
